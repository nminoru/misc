# Ubuntu 24.04 へ Xfce4 をインストールする

Azure VM に Ubuntu 24.04 をインストールすると SSH 接続が可能となるが、X Window System をインストールし RDP 接続も可能にする。

```sh
install-xfce4-on-ubuntu-24-04.sh
```

一緒に開発ツールをインストールする

```sh
install-xfce4-on-ubuntu-24-04.sh chrome code dbeaver 
```
